{
  "name": "xola-oauth-sample-app",
  "version": "1.0.0",
  "description": "OAuth2 sample client for testing Xola's OAuth server",
  "main": "server.js",
  "scripts": {
    "start": "node --inspect server.js",
    "dev": "node server.js",
    "build:gh-pages": "node scripts/build-gh-pages.js",
    "deploy:gh-pages": "npm run build:gh-pages && npx --yes gh-pages -d dist"
  },
  "keywords": [
    "oauth2",
    "pkce",
    "xola"
  ],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=16.0.0"
  },
  "dependencies": {
    "axios": "^1.6.2",
    "cookie-session": "^2.1.1",
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  }
}
