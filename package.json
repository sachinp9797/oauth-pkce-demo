{
  "name": "oauth-pkce-demo",
  "version": "1.0.0",
  "description": "OAuth 2.0 authorization code flow with PKCE — runs in any browser, works with any compliant server",
  "main": "server.js",
  "scripts": {
    "start": "node --inspect server.js",
    "dev": "node server.js",
    "build:gh-pages": "node scripts/build-gh-pages.js",
    "deploy:gh-pages": "npm run build:gh-pages && npx --yes gh-pages -d dist"
  },
  "keywords": ["oauth2", "pkce", "authorization-code-flow"],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=16.0.0"
  },
  "dependencies": {
    "axios": "^1.6.2",
    "cookie-session": "^2.1.1",
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  }
}
